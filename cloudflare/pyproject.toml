[project]
name = "wallet-vitals-worker"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = ["fastapi", "httpx==0.28.1", "jinja2", "pydantic"]

[dependency-groups]
dev = ["workers-py", "workers-runtime-sdk"]
